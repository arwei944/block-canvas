{
  "name": "block-canvas",
  "version": "3.1.0",
  "private": true,
  "description": "A block-based canvas editor monorepo",
  "scripts": {
    "dev": "pnpm --filter @block-canvas/playground dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
